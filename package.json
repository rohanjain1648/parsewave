{
  "name": "novaswarm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "NOVA SWARM — a neon roguelite wave-survivor built for the Parsewave Game Jam.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "smoke": "node test/smoke.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.185.1",
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
